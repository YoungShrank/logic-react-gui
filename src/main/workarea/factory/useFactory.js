import { handleContrlBByAssist } from "./eventHandler";
function useFactory(name)
{
    switch (name){
        case "ASSIST":
            return {}

            
        default:
            return {}
    }
}

export default useFactory;